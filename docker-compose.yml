services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: quackhack2026:latest
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_DIALECT: sqlite
    depends_on:
      db:
        condition: service_healthy
